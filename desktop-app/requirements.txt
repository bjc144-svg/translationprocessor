# Translation Processor Desktop App Requirements

# Core Document Processing (REQUIRED)
python-docx==1.1.0
lxml>=4.9.0

# PDF Conversion (REQUIRED - requires Microsoft Word)
docx2pdf==0.1.8
pywin32==306

# Image Processing (for logos/signatures)
Pillow>=9.0.0

# Optional - Drag and Drop support
# If this fails to install, app will still work with Browse button
# Uncomment to enable drag-and-drop:
# tkinterdnd2==0.3.0
